---
// Pas de props pour l'instant — contenu statique
---

<section
  class="px-10 pt-16 pb-14 border-b border-thin"
  style="border-color: color-mix(in srgb, var(--color-macarel-pierre) 50%, transparent);"
>
    <div class="container mx-auto">
      <span class="section-label">Services</span>
 
        <h1 class="text-display mt-4 mb-5 max-w-prose" style="color: var(--color-macarel-encre);">
            Trois offres.<br>
            <em class="not-italic" style="color: var(--color-macarel-argile);">
            Une seule obsession.
            </em>
        </h1>
        
        <p class="text-md leading-loose max-w-content" style="color: var(--color-macarel-schiste);">
            Outils métier, SaaS, accompagnement technique.
            Dans les trois cas, le même engagement : du code propre,
            livré à temps, que vous pouvez faire évoluer sans moi si vous voulez.
        </p>
    </div>
</section>